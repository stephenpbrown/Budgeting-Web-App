@model IEnumerable<BudgetingWebApp.Models.BudgetModel>

@{
    ViewBag.Title = "Budgets";
}

<h2>Budgets</h2>

<p>
    @Html.ActionLink("Create New Budget", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DateCreated)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BudgetMonth)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DateCreated)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BudgetMonth)
        </td>
        <td>
            @*@Html.ActionLink(
                linkText: "Edit",
                actionName: "Index",
                controllerName: "MainCategory",
                routeValues: new
                {
                    id = item.BudgetID
                },
                htmlAttributes: null
            ) |*@
            @Html.ActionLink("View", "Edit", new { id=item.BudgetID }) |
            @*@Html.ActionLink("Details", "Details", new { id=item.BudgetID }) |*@
            @Html.ActionLink("Delete", "Delete", new { id=item.BudgetID })
        </td>
    </tr>
}

</table>
